# Drop this inside your existing server { ... } block for saleem.net

location ^~ /life-hacks/ {
    alias /var/www/life-hacks/;
    index index.html;
    try_files $uri $uri/ =404;
    autoindex off;
}

# Optional staging path for initial verification
# location ^~ /life-hacks-test/ {
#     alias /var/www/life-hacks-test/;
#     index index.html;
#     try_files $uri $uri/ =404;
#     autoindex off;
# }
